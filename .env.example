# JIRA connection (use for connector.py or load with python-dotenv in your app)
# Copy to .env and fill in your values. Do not commit .env.

# Base URL of your JIRA (no trailing slash)
JIRA_SERVER=https://your-domain.atlassian.net

# For Jira Cloud: use email + API token
# Create token: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_EMAIL=you@example.com
JIRA_API_TOKEN=your-api-token

# For Jira Server / Data Center: use username + password
# JIRA_USERNAME=your_username
# JIRA_PASSWORD=your_password
